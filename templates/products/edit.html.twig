{% extends 'base.html.twig' %}

{% block title %}Edit Products{% endblock %}

{% block body %}
    <div class="container">
        <div class="card mt-5">
            <div class="card-header">
                <h3>Modifier ce produit</h3>
            </div>
            <div class="card-body">
                {# {{ include('products/_form.html.twig', {'button_label': 'Modifier'}) }} #}
                {{ form_start(form) }}
                {# <form method="POST" action="" onSubmit={}> #}
                    <div class="form-group">
                        {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.prixV, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.stockI, {'attr': {'class': 'form-control '}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.stockLivrer, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {# <label for="stockFinal" class="form-label">Stock final</label> #}
                        {# <input type="number" name="stockFinal" class="form-control" id="stockFinal" value={{product.StockFinal}}> #}
                        {{ form_row(form.stockFinal, {'attr': {'class': 'form-control', 'id': 'disableSockFinal'}}) }}
                    </div>
                    <button class="btn {{ button_color|default('btn-outline-primary') }}">{{ button_label|default('Modifier') }}</button>
                {# </form> #}
                {{ form_end(form) }}

            </div>
            <div class="card-footer">
                <a class="btn btn-outline-info float-right" href="{{ path('app_products_index') }}">
                    Retour
                </a>
            </div>
        </div>
    </div>
    {# <h3>Modifier ce produit</h3>

    

    <a href="{{ path('app_products_index') }}">Retour</a>

    {{ include('products/_delete_form.html.twig') }} #}
{% endblock %}
